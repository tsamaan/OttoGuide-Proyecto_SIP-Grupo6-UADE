# Copiar a .env y ajustar la IP del robot.
# URL del backend que corre en el robot (puerto 3000), accesible por el cable RJ45.
VITE_ROBOT_BASE_URL=http://192.168.123.164:3000
# Arranca en modo simulacion (datos falsos, sin robot). Cambiar a false para usar el robot real.
VITE_MOCK_MODE=true
