# Offline Navigation Sandbox — Readiness

**Fecha**: 2026-06-21
**Fase**: Fase 2H.1.3 (hereda 2H.0/2H.1/2H.1.2): cierre de brechas de estado degradado en `close()`, propagación de errores de cleanup y validación estricta de evidencia en el smoke runtime de `DirectNav2ActionBridge`, validados de forma aislada del runtime principal. Fase 2H.0 original: Reconciliación de contratos de navegación y hardware. `TourOrchestrator` ahora depende de los contratos canónicos abstractos `hardware.interface.RobotHardwareInterface` y `src.navigation.port.NavigationPort` en lugar de implementaciones concretas (`RobotHardwareAPI`, `AsyncNav2Bridge`). No cambia comportamiento de navegación ni de hardware; no ejecuta runtime ROS; no toca el sandbox (`offline_nav_sandbox.launch.py`/`nav2_offline_sandbox_params.yaml` sin cambios). Hereda el estado de Fase 2G: Waypoint Follower aislado (`FollowWaypoints` únicamente) integrado en el sandbox offline, sumado a `bt_navigator` (`NavigateToPose`) + `behavior_server` (`Wait`, `Spin`) + `controller_server` + `collision_monitor` + local costmap de fases previas. `waypoint_follower` envía un goal `NavigateToPose` por waypoint al `bt_navigator` ya validado en Fase 2F; nunca publica velocidad directamente. No incluye `NavigateThroughPoses`, Simple Commander, `BackUp`, `DriveOnHeading`, `AssistedTeleop` ni el orquestador de misión de la aplicación paralela (`BasicNavigator.followWaypoints()`).

Este documento separa el estado de readiness del sandbox en niveles independientes. Ningún nivel implica automáticamente el siguiente.

## Niveles de readiness

| Nivel | Estado | Evidencia |
|---|---|---|
| `STATIC_BASELINE` | **READY** | Sandbox existente auditado (`offline_nav_sandbox.launch.py`, `nav2_offline_sandbox_params.yaml`); mapa sintético versionado creado y marcado `SYNTHETIC_TEST_MAP`/`NOT_UADE_MAP`/`NOT_METRICALLY_VALIDATED`/`NOT_FOR_PHYSICAL_NAVIGATION`; default del launch apunta al mapa versionado (no a `artifacts/`); verificador de aislamiento estático (`verify_sandbox_isolation.py`) implementado y en `PASS`; tests `unittest` puros sin ROS. |
| `LOCAL_ROS_CAPABILITIES` | **READY** | Matriz de paquetes ROS 2 locales verificada en WSL `Ubuntu-24.04` (ROS 2 `jazzy`) sin `apt`/`pip`/`rosdep`/Internet — ver [OFFLINE_NAVIGATION_LOCAL_CAPABILITIES.md](OFFLINE_NAVIGATION_LOCAL_CAPABILITIES.md). Todos los paquetes Nav2 base requeridos para fases posteriores están presentes, salvo `nav2_loopback_sim` (no instalado). |
| `ROS_RUNTIME_SANDBOX` | **PARTIAL** | Se levantó una base de runtime ROS real y aislada: `map_server` + `planner_server` + `controller_server` + `collision_monitor` + `behavior_server` (`Wait`/`Spin`) + `bt_navigator` (`NavigateToPose`) + `waypoint_follower` (`FollowWaypoints`) + lifecycle managers separados (uno por componente) + `offline_runtime_simulator` (odometría y scan sintéticos) + TF estático y dinámico completo, todo bajo namespace real `offline_nav`, `ROS_LOCALHOST_ONLY=1` y `ROS_DOMAIN_ID` dedicado. Los smoke tests ROS pasaron. Sigue `PARTIAL` porque falta Simple Commander/orquestador de misión (Collision Monitor, Behavior Server, BT Navigator y ahora Waypoint Follower ya están incluidos y verificados) — ver [OFFLINE_NAVIGATION_SANDBOX_RUNTIME_RUNBOOK.md](OFFLINE_NAVIGATION_SANDBOX_RUNTIME_RUNBOOK.md). |
| `GLOBAL_PLANNING_SANDBOX` | **READY** | `planner_server` con plugin `nav2_navfn_planner::NavfnPlanner` y `global_costmap` (static layer + inflation layer) activos bajo namespace real. Global Planning fue validado originalmente sin Controller, pero el runtime integrado actual sí lo contiene. La acción `/offline_nav/compute_path_to_pose` devolvió `SUCCEEDED` con 59 poses en frame `map`, en dos ejecuciones independientes, sin `/cmd_vel`/`/cmd_vel_nav`, sin nodos de hardware, y sin procesos huérfanos. Detalle completo en [OFFLINE_NAVIGATION_GLOBAL_PLANNING_REPORT.md](OFFLINE_NAVIGATION_GLOBAL_PLANNING_REPORT.md). No implica navegación autónoma. |
| `LOCAL_CONTROL_SANDBOX` | **READY** | `controller_server` + `local_costmap` + simulador con integración cinemática y allowlist de velocidad (`cmd_vel_raw` y `cmd_vel_safe` permitidos) están implementados. Local Control ya utiliza la cadena `cmd_vel_raw` -> `collision_monitor` -> `cmd_vel_safe` -> `offline_runtime_simulator`, donde el simulador consume `cmd_vel_safe` y no `cmd_vel_raw`. La carga incorrecta de parámetros anidados de plugin y la observación incorrecta de topics de odometría/comandos están completamente resueltas. El escenario de éxito confirma avance de pose simulada y llegada al objetivo. El escenario de cancelación confirma parada cinemática exitosa (`STATUS_CANCELED`), detención efectiva por watchdog y estabilidad de pose. Detalle completo en [OFFLINE_NAVIGATION_LOCAL_CONTROL_REPORT.md](OFFLINE_NAVIGATION_LOCAL_CONTROL_REPORT.md). |
| `COLLISION_SAFETY_SANDBOX` | **READY** | `collision_monitor` configurado y activo bajo namespace real, filtrando comandos en la cadena `controller_server`/`behavior_server` -> `cmd_vel_raw` -> `collision_monitor` -> `cmd_vel_safe` -> `offline_runtime_simulator`. Cinco escenarios (Clear, Slowdown, Stop, Recovery, Cancel) validados de forma independiente y reproducible en dos corridas completas (domain IDs `200`-`204` y `210`-`214`), con emparejamiento causal raw/safe (no basado en el resultado esperado): mediana CLEAR `0.9737`/`1.0`, mediana SLOWDOWN `0.40`/`0.4222`, ambas con ≥17 pares válidos. `NOT_FOR_PHYSICAL_SAFETY_VALIDATION`. Detalle completo en [OFFLINE_NAVIGATION_COLLISION_MONITOR_REPORT.md](OFFLINE_NAVIGATION_COLLISION_MONITOR_REPORT.md). |
| `BEHAVIOR_SERVER_SANDBOX` | **READY** | `behavior_server` configurado y activo bajo namespace real con plugins `nav2_behaviors::Wait` y `nav2_behaviors::Spin` (`max_rotational_vel=0.30 rad/s`), remapeando `cmd_vel` -> `cmd_vel_raw` igual que `controller_server`, con `lifecycle_manager_behavior_server` dedicado. Tres escenarios (Wait, Spin, Cancel Spin) validados de forma independiente y reproducible en dos corridas completas (domain IDs `160`-`162` y `170`-`172`), confirmando que la salida de `behavior_server` pasa íntegramente por `collision_monitor` antes de llegar al simulador. `NOT_FOR_PHYSICAL_SAFETY_VALIDATION`. Detalle completo en [OFFLINE_NAVIGATION_BEHAVIOR_SERVER_REPORT.md](OFFLINE_NAVIGATION_BEHAVIOR_SERVER_REPORT.md). |
| `BT_NAVIGATOR_SANDBOX` / `NAVIGATE_TO_POSE_SANDBOX` | **READY** | `bt_navigator` (`nav2_bt_navigator::NavigateToPoseNavigator` únicamente) configurado y activo bajo namespace real, con `lifecycle_manager_bt_navigator` dedicado, sin remaps de velocidad, orquestando un árbol mínimo versionado (`ComputePathToPose -> FollowPath`, sin recoveries). La acción `/offline_nav/navigate_to_pose` devolvió `SUCCEEDED` en el escenario de éxito (avance real de pose, llegada al objetivo dentro de `0.12m`) y `CANCELED` en el escenario de cancelación (con `cmd_vel_safe`/twist de odometría asentados en cero y pose estable tras cancelar), ambos validados en dos corridas completas e independientes (domain IDs `180`-`181` y `190`-`191`) sin cambios de código entre ellas. Confirmado por introspección ROS que `bt_navigator` nunca publica velocidad directamente. `NOT_FOR_PHYSICAL_SAFETY_VALIDATION`. Detalle completo en [OFFLINE_NAVIGATION_BT_NAVIGATOR_REPORT.md](OFFLINE_NAVIGATION_BT_NAVIGATOR_REPORT.md). |
| `WAYPOINT_FOLLOWER_SANDBOX` / `FOLLOW_WAYPOINTS_SANDBOX` | **READY** | `waypoint_follower` (`nav2_waypoint_follower`, plugin `wait_at_waypoint`/`WaitAtWaypoint`, `stop_on_failure=true`) configurado y activo bajo namespace real, con `lifecycle_manager_waypoint_follower` dedicado, sin remaps de velocidad. La acción `/offline_nav/follow_waypoints` devolvió `SUCCEEDED` en el escenario de éxito (3 waypoints, progresión de feedback exactamente `[0,1,2]`, `missed_waypoints=[]`), `CANCELED` en el escenario de cancelación (cancelación real vía `action_msgs/srv/CancelGoal` aceptada y verificada, tras observar movimiento real pasado el primer waypoint), y `ABORTED` en el escenario de waypoint inalcanzable (punto fuera de los límites del mapa, `missed_waypoints=[1]`, `MissedWaypoint.error_code=204`/`GOAL_OUTSIDE_MAP`, sin progresar al waypoint posterior — prueba de `stop_on_failure=true`), los tres validados en dos corridas completas e independientes (domain IDs `200`-`202` y `210`-`212`) sin cambios de código entre ellas. Confirmado por introspección ROS que `waypoint_follower` nunca publica velocidad directamente. `NOT_FOR_PHYSICAL_SAFETY_VALIDATION`. Detalle completo en [OFFLINE_NAVIGATION_WAYPOINT_FOLLOWER_REPORT.md](OFFLINE_NAVIGATION_WAYPOINT_FOLLOWER_REPORT.md). |
| `PHYSICAL_NAVIGATION` | **NOT_READY** | Sin cambios respecto al estado previo. Depende de L2 y L3 validados, de calibración TF física, de mapa navegable completo y de los criterios de la sección 7 del plan original. No se conectó el robot ni se usó hardware físico en esta fase. |
| `ARCHITECTURE_RECONCILIATION` | **READY** | `TourOrchestrator` tipa `hardware_api` contra `hardware.interface.RobotHardwareInterface` y `nav_bridge` contra `src.navigation.port.NavigationPort`; ya no importa `src.hardware` ni `AsyncNav2Bridge`. `NavWaypoint`/`NavigationStatus` se extrajeron a `src/navigation/models.py` (puro, sin ROS) preservando compatibilidad de import desde `src.navigation` y `src.navigation.nav2_bridge`. La implementación inyectada en runtime sigue siendo la misma (`AsyncNav2Bridge` legacy); no cambió comportamiento de navegación ni de hardware. Detalle completo en [ARCHITECTURE_RECONCILIATION_2H0_REPORT.md](../../Arquitectura/ARCHITECTURE_RECONCILIATION_2H0_REPORT.md) y [ADR_002_RECONCILIACION_NAVEGACION_HARDWARE.md](../../Arquitectura/ADR_002_RECONCILIACION_NAVEGACION_HARDWARE.md). |
| `DIRECT_NAV2_ACTION_BRIDGE_OFFLINE` | **READY** | `DirectNav2ActionBridge` (`src/navigation/direct_nav2_action_bridge.py`) validado de forma aislada (Fase 2H.1, endurecido en 2H.1.2: ownership terminal, cancelación, timeouts y estado remoto desconocido corregidos; endurecido en 2H.1.3: deteccion de degradacion preexistente en `close()`, propagacion de errores de cleanup en el smoke, validacion estricta del escenario inalcanzable, integridad de los JSON de procesos hijos). No publica ni se suscribe a velocidad; solo publica `/initialpose`. Confirmado por introspección real de `ros2 node info` que el nodo del bridge no tiene publishers/subscribers de ningún tópico de velocidad. **No conectado a `main.py`/`TourOrchestrator`** — eso es exclusivamente trabajo de la Fase 2H.2. Detalle completo en [DIRECT_NAV2_ACTION_BRIDGE_2H1_REPORT.md](../../Arquitectura/DIRECT_NAV2_ACTION_BRIDGE_2H1_REPORT.md). |
| `DIRECT_NAVIGATE_TO_POSE_CLIENT` | **READY** | `ActionClient` directo contra `/offline_nav/navigate_to_pose`. Escenarios de éxito y cancelación validados en dos corridas oficiales independientes (domain IDs `220`-`223` y `224`-`227`), sin cambios de código entre ellas. Éxito: `SUCCEEDED`, llegada dentro de `0.12m`, `feedback_count>0`, `>=2` muestras de `distance_remaining` con reducción neta. Cancelación: `CANCELED`, `cancel_requested`/`cancel_accepted=true`, UUID coincidente, twists finales en cero, pose estable. |
| `DIRECT_FOLLOW_WAYPOINTS_CLIENT` | **READY** | `ActionClient` directo contra `/offline_nav/follow_waypoints`. Escenario de éxito (3 waypoints, progresión de feedback exactamente `[0,1,2]`, `missed_waypoints=[]`) y escenario inalcanzable (`ABORTED`, `missed_waypoints=[1]`, `error_code=204`/`GOAL_OUTSIDE_MAP`, sin progresar al índice 2) validados en las mismas dos corridas oficiales. |
| `DIRECT_CANCEL_CONTRACT` | **READY** | `cancel_navigation()` exige terminal `CANCELED` confirmado (lanza si no lo es) y nunca se auto-espera desde el monitor de resultado; `_request_cancel_only()` (helper interno) nunca espera `_active_result_task`. Guard estático AST en `verify_sandbox_isolation.py` impide regresiones de este ownership. |
| `DIRECT_RESULT_OBSERVABILITY` | **READY** | `NavigationStatus.remote_state_unknown` expone explícitamente cuándo el bridge no puede confirmar el estado remoto (goal-response timeout, result timeout sin terminal, excepción post-aceptación sin terminal); en esos casos el bridge permanece bloqueado para nuevos goals hasta `close()`. |

## Estado de capas del sistema (sin cambios en esta fase)

| Nivel | Estado | Fuente |
|---|---|---|
| L0 sensores | READY | [PROGRESO_ODOMETRIA_OFFLINE.md](PROGRESO_ODOMETRIA_OFFLINE.md) |
| L1 intención/movimiento | READY | [PROGRESO_ODOMETRIA_OFFLINE.md](PROGRESO_ODOMETRIA_OFFLINE.md) |
| L2 odometría | **NOT_READY** | No existe trayectoria offline confiable; sin cambios en esta fase |
| L3 localización/mapa | **NOT_READY** | Depende de L2 validado; sin cambios en esta fase |
| Navegación física | **NOT_READY** | Depende de L2, L3, calibración TF y criterios de la sección 7 del plan |

## Lo que esta fase NO declara

- No declara autonomía validada.
- No declara mapa físico validado (el mapa sintético es exclusivamente para tests offline, no representa la UADE).
- No declara odometría validada (la odometría es sintética e integrada deterministamente en 2D en el simulador del sandbox, no representa validación física de L2).
- No declara localización ni mapa validados (las TF `map`→`odom` y `base_link`→`utlidar_lidar` son identidades sintéticas, no extrínsecos medidos).
- No declara Nav2 listo para el robot.
- No declara navegación autónoma: `GLOBAL_PLANNING_SANDBOX = READY` significa exclusivamente que `planner_server` calcula rutas candidatas sobre un mapa sintético (validado originalmente sin control). Actualmente, aunque el runtime incluye `controller_server`, el control local está READY solo dentro del sandbox y la odometría es sintética; `PHYSICAL_NAVIGATION` continúa `NOT_READY`.
- No declara `PHYSICAL_NAVIGATION` como `READY` or `PARTIAL`.
- No declara `ROS_RUNTIME_SANDBOX` como `READY` (permanece `PARTIAL`: falta Simple Commander/orquestador de misión; Collision Monitor, Behavior Server con `Wait`/`Spin`, BT Navigator con `NavigateToPose` y ahora Waypoint Follower con `FollowWaypoints` sí están incluidos y validados).
- No declara `NavigateThroughPoses` configurado ni probado; solo `NavigateToPose` (vía `bt_navigator`, también orquestado por `waypoint_follower` un goal por waypoint) está en alcance de esta fase.
- No declara que `RegulatedPurePursuitController` fue probado o está activo; el plugin que este entorno carga realmente es `dwb_core::DWBLocalPlanner`, configurado y activo exclusivamente en este sandbox.
- No declara `BackUp`, `DriveOnHeading` ni `AssistedTeleop` configurados o probados; solo `Wait` y `Spin` están en alcance de esta fase.
- No declara que el orquestador de misión de la aplicación paralela (`BasicNavigator.followWaypoints()` en `src/navigation/nav2_bridge.py`) fue integrado, modificado o validado contra este sandbox; sigue siendo una arquitectura paralela no integrada. La Fase 2H.0 reconcilió únicamente los *contratos* (`TourOrchestrator` ya no depende de tipos concretos); la integración real vía cliente ROS 2 directo queda para la Fase 2H.1.
- No declara `MISSION_ORCHESTRATION`, `DIRECT_NAV2_ACTION_BRIDGE` ni `SIMPLE_COMMANDER` como `READY`; la Fase 2H.0 es exclusivamente de contratos/tipos, no de runtime.
- No declara que el plugin `wait_at_waypoint` ejecuta ninguna acción real en cada waypoint: `waypoint_pause_duration=0` deshabilita explícitamente el task executor (confirmado en logs: "Waypoint pause duration is set to zero, disabling task executor plugin").
- No declara que `DirectNav2ActionBridge` está conectado a `main.py`/`TourOrchestrator`: `MAIN_RUNTIME_MIGRATED=NO`, `LEGACY_NAVIGATION_RUNTIME_ACTIVE=YES`. La selección del bridge directo como implementación inyectada en producción es exclusivamente trabajo de la Fase 2H.2, todavía no autorizada.
- No declara `PHYSICAL_READINESS_CHANGED`: ni la Fase 2H.1.2 ni la 2H.1.3 modificaron L2/L3/`PHYSICAL_NAVIGATION`, que permanecen `NOT_READY` sin cambios.

## Qué cambió respecto a la fase previa

- Se documentó por primera vez, con evidencia verificada localmente, qué paquetes Nav2 existen en el entorno de desarrollo (Fase 1).
- Se reemplazó la dependencia del launch en un artefacto local no versionado (`artifacts/maps/ottoguide_hil_stationary_map.yaml`) por un mapa sintético versionado y explícitamente no apto para navegación física (Fase 1).
- Se agregó un verificador de aislamiento estático reproducible y tests puros asociados (Fase 1).
- Se levantó por primera vez runtime ROS real bajo aislamiento (`ROS_LOCALHOST_ONLY=1`, `ROS_DOMAIN_ID` dedicado), con namespace real `offline_nav` aplicado a los nodos, un simulador sintético de odometría/scan, y TF estático/dinámico completo (Fase 2A).
- Se agregó un verificador de aislamiento en modo `--runtime` (variables de entorno ausentes o incorrectas pasan de warning a error) y un smoke test ROS que confirmó mensajes en los tres tópicos esperados y cierre sin procesos huérfanos (Fase 2A).
- Se agregó `planner_server` (plugin `nav2_navfn_planner/NavfnPlanner`) y `global_costmap` (static + inflation layer) bajo el mismo namespace real y aislamiento, activados por el mismo `lifecycle_manager` que `map_server`, sin `controller_server` ni componentes de movimiento (Fase 2B).
- El verificador de aislamiento ahora valida namespace/remapping por entidad individual (cada `Node`/`ExecuteProcess` requerido, no solo un conteo `>0`) y distingue código que *detecta* `/cmd_vel`/`/cmd_vel_nav` como ausentes de código que los *usa* realmente (Fase 2B).
- El wrapper corrige la captura del exit code del launch para que se propague correctamente incluso si es distinto de cero (Fase 2B).
- El smoke test de foundation ahora inicia el runtime mediante el wrapper aislado (no `ros2 launch` directo) y verifica el cierre del process group propio mediante sondeo real, no solo `wait()` (Fase 2B).
- Se agregó un smoke test ROS real de planificación (`smoke_test_offline_planner.py`) que envía un goal `ComputePathToPose` y valida el path resultante; pasó en dos ejecuciones independientes con domain IDs distintos (Fase 2B).
- Se agregó `controller_server` + `local_costmap` al launch y a los parámetros, con remap `cmd_vel` → `cmd_vel_raw`, y se extendió el simulador con suscripción a `cmd_vel_raw`, integración cinemática planar, límites sintéticos y watchdog (Fase 2C).
- Se separó el lifecycle de `controller_server` en un `lifecycle_manager_controller` dedicado, independiente de `lifecycle_manager_navigation` (`map_server`/`planner_server`), para que una falla de activación del controller no regresione el resto del sandbox ya validado (Fase 2C).
- Se descubrió y documentó una incompatibilidad real en el launch (Fase 2C): los parámetros anidados bajo el namespace de un plugin (`FollowPath.plugin`, `FollowPath.critics`, y también `GridBased.tolerance` de la Fase 2B sin que se notara antes) no se aplicaban desde `--params-file` ni desde `-p` porque el launch no reescribía el archivo de parámetros con el namespace real como raíz.
- Se corrigió la causa raíz (corrección posterior a la Fase 2C): `offline_nav_sandbox.launch.py` ahora reescribe los parámetros con `ParameterFile(RewrittenYaml(source_file=PARAMS_FILE, root_key=namespace, convert_types=True), allow_substs=True)`, aplicado a `map_server`, `planner_server` y `controller_server`. `controller_server` alcanza `active` de forma reproducible y `/offline_nav/follow_path` devuelve `SUCCEEDED`. Se corrigió además `GridBased.plugin` en el YAML al namespace C++ real (`nav2_navfn_planner::NavfnPlanner` en vez del alias `nav2_navfn_planner/NavfnPlanner`), error que quedó expuesto recién al aplicarse realmente el parámetro.
- El verificador de aislamiento ahora exige una allowlist estricta de velocidad: solo `cmd_vel_raw` (resuelve a `/offline_nav/cmd_vel_raw`) está permitido; `/cmd_vel`, `/cmd_vel_nav` y `/offline_nav/cmd_vel` quedan rechazados explícitamente (Fase 2C).
- Se integró `nav2_collision_monitor` bajo namespace real `/offline_nav` y un `lifecycle_manager_collision_monitor` aislado, configurado con zonas slowdown y stop sintéticas. El verificador estático y los unit tests se actualizaron para validar la cadena de tópicos raw y safe, prohibir bypasses directos y comprobar las suscripciones correctas del simulador (Fase 2D).
- Se creó `smoke_test_offline_collision_monitor.py` para validar de forma automatizada e independiente en domain IDs aislados los escenarios de Clear, Slowdown (velocidad segura reducida al 40%), Stop (velocidad segura cero, pose estable), Recovery (stop -> clear) y Cancel (Fase 2D).
- Se endureció el emparejamiento causal raw/safe del smoke test de Collision Monitor: cada muestra `cmd_vel_safe` se empareja con la muestra `cmd_vel_raw` más reciente con timestamp anterior o igual (delta máximo `0.25s`), sin reutilizar muestras safe y descartando pares con `abs(raw) < 0.02`; la mediana de ratios se calcula sobre el conjunto completo de pares válidos, nunca seleccionando muestras por cercanía al resultado esperado. Se corrigió además que un timeout transitorio de `ros2 lifecycle get` (bajo carga acumulada de lanzamientos secuenciales) abortara el escenario con una excepción en vez de permitir que el bucle de reintento existente continuara hasta su propio deadline (Fase 2D.1).
- Se integró `nav2_behaviors/behavior_server` bajo namespace real `/offline_nav` con un `lifecycle_manager_behavior_server` dedicado, configurado únicamente con los plugins `nav2_behaviors::Wait` y `nav2_behaviors::Spin` (`max_rotational_vel=0.30 rad/s`, frames sintéticos `odom`/`base_link`). Su salida remapea `cmd_vel` -> `cmd_vel_raw`, igual que `controller_server`, de forma que ambos publishers pasan obligatoriamente por `collision_monitor`. El verificador estático ahora exige el nodo namespaced, su lifecycle manager dedicado, y rechaza cualquier bypass directo a `cmd_vel_safe` o referencia a BT Navigator/Waypoint Follower/Simple Commander en el launch (Fase 2E).
- Se creó `smoke_test_offline_behavior_server.py`, que deriva tres domain IDs desde `--base-domain-id` y valida los escenarios Wait, Spin y Cancel Spin, confirmados en dos corridas independientes y reproducibles sin cambios de código entre ellas (Fase 2E). 144 tests puros en `OK`.
- Se integró `nav2_bt_navigator`/`bt_navigator` bajo namespace real `/offline_nav` con un `lifecycle_manager_bt_navigator` dedicado, configurado únicamente con el navigator `nav2_bt_navigator::NavigateToPoseNavigator` y un árbol de comportamiento mínimo versionado (`offline_navigate_to_pose.xml`: `ComputePathToPose -> FollowPath`, sin recoveries). La ruta del XML se inyecta en runtime vía `param_rewrites` desde una constante derivada de `CODE_ROOT`, nunca hardcodeada en el YAML. `NavigateThroughPoses` no está habilitado. El verificador estático ya no trata a `bt_navigator`/`nav2_bt_navigator` como componente prohibido y agrega `check_bt_navigator_contract` para validar namespace, lifecycle aislado, ausencia de remaps de velocidad y el contenido del XML (Fase 2F).
- Se creó `smoke_test_offline_bt_navigator.py`, que deriva dos domain IDs desde `--base-domain-id` y valida un escenario de éxito (`NavigateToPose` hasta un objetivo calculado dinámicamente sobre la pose inicial real, `SUCCEEDED`, llegada dentro de `0.12m`) y un escenario de cancelación (precondición de movimiento real antes de cancelar, `CANCELED`, velocidad y twist asentados en cero, pose estable), confirmados en dos corridas independientes y reproducibles sin cambios de código entre ellas (Fase 2F). Se corrigió además la detección de movimiento del escenario Wait de Behavior Server para evaluar los tres componentes planares (`linear.x`, `linear.y`, `angular.z`) en vez de solo `angular.z`, y se agregó validación explícita de rango de domain IDs (`1..232`, con offset máximo por componente) a los tres smoke tests multi-escenario (Behavior Server, Collision Monitor, BT Navigator). 195 tests puros en `OK`.
- Se integró `nav2_waypoint_follower`/`waypoint_follower` bajo namespace real `/offline_nav` con un `lifecycle_manager_waypoint_follower` dedicado, configurado con `stop_on_failure=true` y `waypoint_task_executor_plugin="wait_at_waypoint"` (`nav2_waypoint_follower::WaitAtWaypoint`, `waypoint_pause_duration=0`, el plugin estándar más simple sin efectos laterales). Envía un goal `NavigateToPose` por waypoint al `bt_navigator` ya validado en Fase 2F. El verificador estático reemplazó la prohibición absoluta de `waypoint_follower`/Simple Commander por una excepción namespaced y estricta (`check_waypoint_follower_contract`): exige package `nav2_waypoint_follower`, executable `waypoint_follower`, namespace real, lifecycle manager aislado conteniendo únicamente ese nodo, y ausencia de remaps de velocidad; Simple Commander y `BasicNavigator` permanecen completamente prohibidos (Fase 2G).
- Se creó `smoke_test_offline_waypoint_follower.py`, que deriva tres domain IDs desde `--base-domain-id` (success/cancel/unreachable) y valida: éxito con 3 waypoints sintéticos (progresión de feedback normalizada exactamente `[0,1,2]`, sin huecos ni retrocesos, `missed_waypoints=[]`); cancelación real tras observar movimiento pasado el primer waypoint, verificada contra la respuesta real de `action_msgs/srv/CancelGoal` (mismo contrato endurecido en Fase 2F.1); y un waypoint deliberadamente inalcanzable (fuera de los límites reales del mapa fixture, derivado y confirmado por inspección directa de los píxeles del `.pgm`, no una coordenada arbitraria), probando que `stop_on_failure=true` detiene la ruta exactamente en el índice fallido sin progresar al waypoint siguiente. Cada escenario escribe su log a un archivo dedicado bajo `/tmp` en vez de `stdout=subprocess.PIPE` sin consumidor, evitando un deadlock real del proceso `ros2 launch` si el buffer del pipe se llena (Fase 2G). Durante la reanudación de la Fase 2G se corrigió además: el punto originalmente hipotetizado como "celda ocupada interior" (`(0.025, 0.575)`) resultó ser real pero *reachable* en la práctica (un solo píxel ocupado a 0.05m de resolución es más fino que el footprint+inflation efectivos del planner, que lo rodea exitosamente) — descubierto mediante una corrida ROS diagnóstica real (no asumido), y sustituido por un punto fuera de los límites del mapa, confirmado con `error_code=204`/`GOAL_OUTSIDE_MAP`; y se ampliaron, con autorización explícita puntual, las listas `FORBIDDEN_MISSION_NODE_SUBSTRINGS` de `smoke_test_offline_behavior_server.py` y `smoke_test_offline_bt_navigator.py` (ambos archivos pre-Fase-2G, fuera del allowlist original) para dejar de tratar a `waypoint_follower` como componente de misión prohibido, ya que ambas regresiones fallaban al detectarlo como violación tras su integración legítima; Simple Commander permanece prohibido en ambos archivos. 259 tests puros en `OK` bajo WSL Jazzy (cero skips), 235 en Windows (48 skips esperados, dependientes de `rclpy`).

- Fase 2H.0: se extrajeron `NavWaypoint`/`NavigationStatus` de `src/navigation/nav2_bridge.py` a un módulo puro nuevo (`src/navigation/models.py`, cero imports de ROS), preservando compatibilidad de import desde `src.navigation` y `src.navigation.nav2_bridge` (identidad de clase verificada, no solo igualdad estructural). Se creó `src/navigation/port.py` con el contrato abstracto `NavigationPort` (`Protocol` `runtime_checkable`, también sin imports de ROS), que `TourOrchestrator` ahora usa para tipar `nav_bridge` en lugar de `AsyncNav2Bridge` concreto, y `hardware.interface.RobotHardwareInterface` para tipar `hardware_api` en lugar de `RobotHardwareAPI` (legacy, `src/hardware/`). `AsyncNav2Bridge`, `MockNav2Bridge` y `_MinimalNavStub` (en `main.py`) ya conforman estructuralmente `NavigationPort` (verificado con `isinstance()` real). No se ejecutó runtime ROS, no se tocó el sandbox (`offline_nav_sandbox.launch.py`/`nav2_offline_sandbox_params.yaml` sin cambios), y no cambió ningún comportamiento de navegación o hardware. `src/hardware/` permanece legacy, en cuarentena, sin eliminarse. Se corrigió además el docstring de `smoke_test_offline_waypoint_follower.py`, heredado de la Fase 2G, que describía incorrectamente el escenario UNREACHABLE como una celda ocupada interior (el comentario de código junto a la constante ya tenía la descripción correcta; solo el docstring del módulo estaba desactualizado). Detalle completo en [ARCHITECTURE_RECONCILIATION_2H0_REPORT.md](../../Arquitectura/ARCHITECTURE_RECONCILIATION_2H0_REPORT.md).

- Fase 2H.1.2: auditoría del commit publicado por la Fase 2H.1.1 (`49a998c`) en `DirectNav2ActionBridge`. Se confirmaron y corrigieron defectos reales de ownership del estado terminal: auto-espera del monitor de resultado sobre el método público de cancelación, limpieza de estado inferida desde el enum local `ERROR`/`TIMEOUT` en vez de evidencia terminal comprobada, ausencia de exigencia de terminal `CANCELED` tras una cancelación aceptada, manejo inseguro de excepciones posteriores a la aceptación del goal, goal-response timeout sin preservar el UUID generado, y un `except ImportError: pass` que silenciaba una dependencia de pose faltante. Se agregó el campo `NavigationStatus.remote_state_unknown` y un helper interno `_request_cancel_only()` separado del método público `cancel_navigation()`. Tests unitarios ampliados de 32 a 44 casos, con guard estático AST nuevo en `verify_sandbox_isolation.py` (`check_direct_nav2_action_bridge_ownership_contract`) para impedir la regresión de este ownership. El smoke test runtime (`smoke_test_direct_nav2_action_bridge.py`) fue rediseñado con aislamiento real por proceso (un proceso hijo por escenario, cada uno con su propio `ROS_DOMAIN_ID`) y un observador de telemetría con contexto/nodo/thread propios, e inspección real del grafo del nodo del bridge vía `ros2 node info`. Los cuatro contratos (`NavigateToPose` éxito/cancelación, `FollowWaypoints` éxito/inalcanzable) se validaron en dos corridas oficiales independientes (domain IDs `220`-`223` y `224`-`227`) sin cambios de código entre ellas, y las regresiones de BT Navigator y Waypoint Follower (sin modificar) continúan en `PASS`. `DirectNav2ActionBridge` sigue sin conectarse a `main.py`; `MAIN_RUNTIME_MIGRATED` permanece `NO`. Detalle completo en [DIRECT_NAV2_ACTION_BRIDGE_2H1_REPORT.md](../../Arquitectura/DIRECT_NAV2_ACTION_BRIDGE_2H1_REPORT.md).

- Fase 2H.1.3: microincremento aditivo que cierra seis brechas puntuales detectadas tras la auditoría de la propia Fase 2H.1.2, sin reauditar ownership terminal/cancelación/timeout (ya `PASS`). `_cleanup()` ahora detecta degradación preexistente al entrar (`remote_state_unknown` ya en `True`, o `task_active=True` sin goal handle, p.ej. tras un goal-response timeout que nunca crea un result task) antes de cualquier intento reactivo de cancelación/espera; un cierre degradado puede volver a reportar la misma degradación en llamadas posteriores, según lo autorizado por el encargo. El smoke ya no silencia `bridge.close()` con `except Exception: pass` (registra `BRIDGE_CLOSE_FAILED`); `TelemetryObserver.shutdown()` verifica el thread tras `join()` y lanza `OBSERVER_THREAD_STILL_ALIVE` si no terminó; `_shutdown_and_count_orphans()` inicializa `pgid` explícitamente y trata `ProcessLookupError` como 0 huérfanos; el escenario `FollowWaypoints` inalcanzable ya nunca acepta `REJECTED` como sustituto de `ABORTED` ni retorna temprano sin validar el contrato completo (extraído a un validador puro `_validate_fw_unreachable_result`); y los JSON de los procesos hijos del smoke usan una ruta única por invocación (PID + `time.time_ns()` + escenario + dominio), validada por identidad (`scenario`/`domain_id`) y coherencia de exit code antes de aceptarse. Tests unitarios ampliados de 44 a 47 casos en `test_direct_nav2_action_bridge.py`, más dos clases nuevas de guards estáticos y tests de helpers puros en `test_offline_navigation_sandbox_isolation.py`. Diagnóstico (`--base-domain-id 212`) y las dos corridas oficiales (`220`-`223`, `224`-`227`) en `PASS`, sin cambios de código entre ellas; regresiones de BT Navigator y Waypoint Follower (sin modificar) en `PASS`. `DirectNav2ActionBridge` sigue sin conectarse a `main.py`. Detalle completo en [DIRECT_NAV2_ACTION_BRIDGE_2H1_REPORT.md](../../Arquitectura/DIRECT_NAV2_ACTION_BRIDGE_2H1_REPORT.md), sección 8.

## Próximo incremento

Fase 2H.2 — Main Runtime Navigation Bridge Selection (pendiente, no autorizada todavía): seleccionar e inyectar `DirectNav2ActionBridge` (o mantener `AsyncNav2Bridge`) como la implementación de `NavigationPort` usada por `main.py`/`TourOrchestrator`. `ROS_RUNTIME_SANDBOX` permanece `PARTIAL` hasta entonces: falta Simple Commander/orquestador de misión. Tampoco se configuraron los plugins `BackUp`, `DriveOnHeading` ni `AssistedTeleop` de `behavior_server`, ni `NavigateThroughPoses` de `bt_navigator`. La política de misión (reintentos, skip, abort, handover) queda para la Fase 2I, también pendiente.
